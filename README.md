## API in GO with local postgressql database

**Swag init:** creates docs folder with swagger config of endpoints

**go run main.go** : runs api

**NEW**
Docker added. To run: "Docker-compose build", then "docker-compose up"
